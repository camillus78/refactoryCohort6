//the keyword var declares the variable is a name
var name;
// ozzy zainab has been assigned to the variable above
//ozzy zainab is a string because it is in quoatation marks
name = " ozzy zainab";

//is a function which outputs the value above
console.log(name);
// is a function which creates a line break
console.log("<br/>");
//the keyword var declares the variable is a name
var name;
//declaration that the value is ozzy emma otim
//the data type is string because it is quotation marks
name = "ozzy emma otim ";

//the function outputs the value ozzy emma otim
console.log(name);

//the keyword var declares the variable is a name
var name;
//the value of the above variable is marrie jacob ozzy
//it is a string because it is in quotation marks
name = "marrie jacob ozyy";
//the funnction below outputs the value
console.log(name);
//the keyword var declares the variable is a name
var name;
//the value of the declaration is ozzy diane
//it is a string because it is in quotation marks
name = " ozzy diane";
//the function below outputs the value above
console.log(name);
//the value is joanita paul ozzy and it is a string because of the quotation marks
name = "joanita paul ozzy ";
//the function below outputs the value above
console.log(name);
//keyword var declares that age is the variable
var age;
//the keyword var declares the variable is a name
var name;
//the value of var name; is 50
//data type is number is number because there are no quotes
age = 50;
//the value of var name; is assigned rino monica nicholus
name = "rino monica nicholus";
/*

*/
document.write("My name is " +name +" "+ "and I am "+age+" "+" years old");

//keyword var declares the variable is x
var x;
//writes javascript code and is used for testing
document.write(x);
//it has been assigned 'undefined' because the object has been declared but not defined
var x = undefined;
//outputs the above declaration
console.log(x);
