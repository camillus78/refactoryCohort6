//shows that the variable is a name
var name;
//name assigned is ozzy zainab
//the data type is string because of the quatation marks
name = " ozzy zainab";

//this is the funtion that displays the message to the user
console.log(name);
// the function that creates line breaks
console.log("<br/>");
//declares that the variable is a name

var name;
//declares that the name assigned is ozzy emma otim
name = "ozzy emma otim ";

//the function that displays the message to the user when it is run on the terminal
console.log(name);

//declares that the valuable is a name
var name;
//declares that the name assigned is marrie jacob ozzy
name = "marrie jacob ozyy";
//the function that displays the message to the user when it is ran in the terminal
console.log(name);
//declares that the valuable is a name
var name;
//declares that the name assigned is ozzy diane
name = " ozzy diane";

//the function that displays the message to the user when it is ran in the terminal
console.log(name);
//declares that the name assigned is joanita paul ozzy
name = "joanita paul ozzy ";
//the function that displays the message to the user when it is ran in the terminal
console.log(name);

//the keyword declares that the variable is age
var age;

//the keyword declares that the variable is a name
var name;
//the value of the variabe age is 50
//data type - Number because it has no quotes
age = 50;
//the name assigned is rino monica nicholus
name = "rino monica nicholus";
/*

*/
//writes a string of texts to a document
document.write("My name is " +name +" "+ "and I am "+age+" "+" years old");

//declares that the variable is x
var x;
//writes the text to a document
document.write(x);
//declares that the variable x assigned is undefined because it has no value
var x = undefined;
//displays the declared message to the user
console.log(x);
