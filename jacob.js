//Declaring a variable called name. The variable name currently has no value assigned to it.
var name;

//initializing the declared variable name with a string value "ozzy zainab"
name = " ozzy zainab";

//Output the value stored in the variable name onto the console. Output value is a string "ozzy zainab"
console.log(name);

// Output the string "<br/>" onto the console.
console.log("<br/>");

//Declare a variable called name, despite earlier declaration of name, no identifier error because of keyword choice
var name;

//initializing the declared variable name with a string value "ozzy emma otim"
name = "ozzy emma otim ";

//Output the value assigned to variable name. Output value is a string "ozzy emma otim"
console.log(name);

//Declare a variable called name, despite earlier declaration of name, no identifier error because of keyword choice
var name;

//initializing the declared variable name with a string value "marrie jacob ozyy"
name = "marrie jacob ozyy";

//Output the value assigned to variable name. Output value is a string "marrie jacob ozyy"
console.log(name);

//Declare a variable called name, despite earlier declaration of name, no identifier error because of keyword choice
var name;

//initializing the declared variable name with a string value "ozzy diane"
name = " ozzy diane";

//Output the value assigned to variable name. Output value is a string "ozzy diane"
console.log(name);

//Overwrite the value currently assigned to variable name with a string value "joanita paul ozzy"
name = "joanita paul ozzy ";

//Output the value assigned to variable name. Output value is a string "joanita paul ozzy"
console.log(name);

//Declaring a variable called age. The variable name currently has no value assigned to it.
var age;

//Declare a variable called name, despite earlier declaration of name, no identifier error because of keyword choice
var name;

//initializing the declared variable age with a number value 50
age = 50;

//initializing the declared variable name with a string value "rino monica nicholus"
name = "rino monica nicholus";

/*
	The write method defined on the document object adds the string parameter passed to it into the document.
	if one chooses to use this methdd, it should be used onload otherwise it will overwrite all the content.

	The result will be "My name is rino monica nicholus and I am 50 years old"
*/
document.write("My name is " +name +" "+ "and I am "+age+" "+" years old");

//Declaring a variable called x. The variable x currently has no value assigned to it.
var x;

//The variable x is undefined therefore a string "undefined" will be added to the document
document.write(x);

//Declare a variable x and initialized with a value undefined.
var x = undefined;

//Output the value assigned to variable x. Output value is a string "undefined"
console.log(x);
