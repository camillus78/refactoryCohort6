//Declares a variable called name and allocates a space in memory according to it's data type.
var name;
//The variable name is assigned a value called 'ozzy zainabu'.
name = " ozzy zainab";

//The line (console.log(name)) prints the value of the variable name on the screen.the output is a string 'ozzy zainab'.
console.log(name);
// Prints the string (<br/>>) on the screen. 
console.log("<br/>");
//Line 12 declares a variable name and a space is allocated for it in memory.

var name;
//The variable name is assigned a value called ozzy emma otim and is of type string.

name = "ozzy emma otim ";

//Console.log(name) prints the value of the variable name on the screen.In this case it's ozzy emma otim.
console.log(name);

//Declares a variable name and a space is allocated for it in memory.
var name;
//The variable name is assigned a value called marrie jacob ozzy  and is of type string.
name = "marrie jacob ozyy";
//Prints the value of the variable name on the screen.In this case it's marrie jacob ozyy.
console.log(name);
//Declares a variable name and a space is allocated for it in memory.
var name;
//The variable name is assigned a value called ozzy diane  and is of type string.
name = " ozzy diane";
//Prints the value of the variable name on the screen.In this case it's ozyy diane.
console.log(name);
//The variable name is assigned a new value of type string called joanita pual ozzy.This replaces the ozzy diane in memory.
name = "joanita paul ozzy ";
//Prints the value of the variable name on the screen.In this case it's joanita paul ozzy.
console.log(name);
//Declaring a variable called age.A space is allocated in memory  to contain the value of age.
var age;
//Declares a variable name .
var name;
//The variable age is assigned a value of value of type number called 50.
age = 50;
//The variable name is assigned a value of type string called rino monica nicholus.
name = "rino monica nicholus";
/*
The method 'document.write' writes the string 'My name is rino monica nicholus and I am 50 years old into an html document with variables 
name and age replaced by their values respectively
*/
document.write("My name is " +name +" "+ "and I am "+age+" "+" years old");

//Declaring a variable called x.A space is allocated in memory  to contain the value of x.
var x;
//Writes the value of the variable 'x' into an html document
document.write(x);
//Declaring a variable called x whose value is null.
var x = undefined;
//Prints the value of the variable x.There is no value in the output because the value of x is undefinrd
console.log(x);
